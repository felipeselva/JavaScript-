// SPLICE

const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Julia'];

// nomes.splice(indice, delete)
//const removidos = nomes.splice(3, 1);
//const removidos = nomes.splice(3, Number.MAX_VALUE);
// PUSH = const removidos = nomes.splice(nomes.length, 0, 'Luiz', 'Felipe');
// SHIFT = const removidos = nomes.splice(0 , 1);
console.log(nomes, removidos);